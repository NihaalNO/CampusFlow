# Example .env for backend
# Option A: path to Firebase service account JSON file
# FIREBASE_CREDENTIALS=C:\path\to\serviceAccountKey.json

# Option B: raw JSON string (careful with newlines/quoting)
# FIREBASE_CREDENTIALS={"type": "service_account", "project_id": "...", "private_key_id": "...", "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n", "client_email": "..."}

# Postgres database URL (optional if you want to run against Supabase/Postgres)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Other envs
# FLASK_ENV=development
# SECRET_KEY=change-me
